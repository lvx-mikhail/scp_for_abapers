ID: empty
_schema-version: '2.1'
description: Empty
version: 0.0.1
modules:
  - name: app-db
    type: hdb
    path: app-db
    requires:
      - name: hdi_app-db
  - name: app-node
    type: nodejs
    path: app-node
    provides:
      - name: app-node_api
        properties:
          url: '${default-url}'
    requires:
      - name: hdi_app-db
resources:
  - name: hdi_app-db
    properties:
      hdi-container-name: '${service-name}'
    type: com.sap.xs.hdi-container
