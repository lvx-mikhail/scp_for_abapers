ID: approuter
_schema-version: '2.1'
version: 0.0.1
modules:
  - name: appRouter
    type: approuter.nodejs
    path: approuter_appRouter
    parameters:
      disk-quota: 256M
      memory: 256M
    requires:
      - name: lev_router
resources:
  - name: lev_router
    type: org.cloudfoundry.existing-service